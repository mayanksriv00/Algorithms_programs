NOTE: Python Input file used to genererate input is not added.
	  Output of the sorted result in file and input file is also not added since the size was too large

How to run.
	Pass the file name as an argument, Note each file contains 20(the program will also work for n no of lines) lines and nis of n size.
	compilation: g++ <filename.cpp -o <executablename> -std=c++11
		Ex: g++ Quick_sort.coo -o Qusik_sort -std=c++11
	Running program: To run the program pass input file using command line argument
		Syntax: ./<executable filename> <input_file>
			Ex: ./Quick_sort sample1.txt